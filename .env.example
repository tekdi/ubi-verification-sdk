# Endpoint for credential verification
VERIFICATION_API_ENDPOINT=
VERIFICATION_API_ENDPOINT_TOKEN=

# Application port (optional default)
PORT=3000

# Host for the server (use 0.0.0.0 for all interfaces, or 127.0.0.1 for localhost)
HOST=0.0.0.0